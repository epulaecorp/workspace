- type: vertical-stack

  cards:
    - type: entities
      entities:
        - entity: person.fvicente
          secondary_info: last-changed
#      card_mod:
#        style: &sidebar |
#          ha-card {
#            border: 0;
#            margin-bottom: 1em;
#          }

    - type: history-graph
      entities:
        - entity: person.fvicente
#      card_mod:
#        style: *sidebar

    - type: custom:mod-card
      card_mod:
        style:
          hui-horizontal-stack-card$: |
            #root {
              justify-content: space-evenly !important;
              padding: var(--tablet-popup-button-padding);
            }
      card:
        type: horizontal-stack
        cards:
          - type: custom:button-card
            name: Encontrar mi iphone
            icon: mdi:volume-high
            template: icon_name
            tap_action:
              action: call-service
              service: notify.mobile_app_fvicente_phone
              service_data:
                title: "Hey"
                message: "Aquí estoy!"
                data:
                  command: "vibrate"
                  push:
                    sound:
                      name: findmy.wav
                      critical: 1
                      volume: 1

    - type: custom:decluttering-card
      template: map_template
      variables: 
        - device_tracker: device_tracker.fvicente_iphone